var tape = require("tape"),
    barcodetree = require("../");

tape("barcodetree() returns the answer to the ultimate question of life, the universe, and everything.", function(test) {
  // test.equal(barcodetree.barcodetree(), 42);
  test.end();
});
